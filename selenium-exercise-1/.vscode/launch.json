{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Python: all",
      "type": "python",
      "request": "launch",
      "module": "behave",
      "console": "integratedTerminal",
      "args": [
        // "--dry-run",
        "--no-capture",
        "--no-capture-stderr",
        "${workspaceFolder}/features",
        "-t",
        "@RegisterTestData",
        "-f",
        "pretty"
      ]
    }
  ]
}
